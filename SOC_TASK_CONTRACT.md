# Task Contract — Soc_brain client goal #9000010

Triển khai và kiểm thử PoC AG-UI + CopilotKit biệt lập cho soc-control/Soc_brain. Đây là yêu cầu implementation, không chỉ lập kế hoạch. Ràng buộc tuyệt đối UNCOMMITTED: KHÔNG commit/push/merge/deploy; dừng tại handoff bằng chứng uncommitted, không đi delivery. Chỉ làm trong worktree được canonical cấp, không primary checkout, không sửa/stage/xóa/ghi đè dirty work lane khác, không đổi production wiring. Đọc AGENTS.md, SOC_TASK_CONTRACT.md và kiến trúc, kiểm tra git status/provenance trước mutation; risk L (authority/lifecycle boundary). Primary checkout hiện main HEAD 04a6b0817c248935ba92cdac9919c4741521f062, ahead 1 behind 14, tool báo 13 modified/97 untracked; không suy diễn ownership từng file. Contract ở primary chỉ chứa reverse dispatch E2E67 cũ, không phải assignment cho PoC.
Mục tiêu UI: (1) dashboard hiển thị nhiều task đồng thời, tối thiểu 2 fixture task hoạt động đồng thời; (2) chọn task xem status, bước hiện tại, last activity và timeline; (3) chat gửi command tới adapter thử nghiệm; (4) HUMAN_GATE với Approve/Reject; (5) realtime event, reconnect trình duyệt/replay không mất state; (6) canonical FSM là authority DUY NHẤT, UI tuyệt đối không terminalize, merge hoặc phát TASK_COMPLETED. Approve/Reject chỉ ghi event thử nghiệm, không tác động canonical gate/GitHub/merge/delivery. Không cần LLM key hoặc external delivery cho fixture demo.
Dùng tài liệu CHÍNH THỨC AG-UI và CopilotKit, xác minh API/version hiện hành, pin dependency và lockfile trong phạm vi PoC. Đã đọc https://docs.ag-ui.com/concepts/events: CUSTOM(name,value), STATE_SNAPSHOT, RUN_STARTED/RUN_FINISHED là protocol run không được đồng nhất canonical task completion. npm view trả @ag-ui/core và @ag-ui/client 0.0.59, @copilotkit/react-core và @copilotkit/react-ui 1.72.0; chưa xác minh compatibility hoặc docs CopilotKit. @ag-ui/server không tồn tại (404). Node v22.23.2; pnpm shim lỗi ERR_MODULE_NOT_FOUND, dùng package manager khả dụng sau kiểm tra, không sửa global environment.
Adapter chỉ đọc canonical state nếu xác minh interface an toàn; nếu chưa có dùng fixture GẮN NHÃN RÕ, không giả dữ liệu thật. packages/control-ui/ui-view-model.mjs đã đọc: buildTaskViewModel compose runtime-sandbox session + task-progress + soc-score + executor-launcher; canonicalState nguyên văn FSM, projection không có authority; evaluate import side-effects/boundaries trước dùng. Event contract tối thiểu task.snapshot, task.status_changed, task.progress, task.log, human_gate.requested, human_gate.resolved, executor.stalled; mapping AG-UI schema thực, không custom UI mang tên CopilotKit nhưng không dùng thư viện. Có replay cursor/dedupe/snapshot recovery, task identity isolation, command allowlist, checkpoint binding và stale/replayed gate handling; không endpoint cho terminalization. Fixture actions không mutate canonical lifecycle.
Test mapping event, reconnect/replay, task isolation, cấm terminalization từ UI/backend; thực hiện targeted → related regression → một full suite theo AGENTS.md, lint/typecheck nếu được định nghĩa. Không skip/drop/raise timeout để qua test. Smoke browser thật trên localhost, inspect console/network và chụp ảnh UI; chứng minh đa task, chọn task, chat, Approve/Reject, realtime và reconnect/reload. Chỉ bind loopback, không mở public. Báo tiến độ Bước X/6 và todo checklist: 1 provenance/kiến trúc, 2 docs/API, 3 adapter+tests, 4 UI, 5 verification/smoke/ảnh, 6 handoff. Báo cáo cuối bằng tiếng Việt gồm kiến trúc PoC, toàn bộ file tạo/sửa, lệnh chạy, URL localhost thực, test/smoke evidence (commands, totals pass/fail/skip/cancel, exit, elapsed, not-ok, logs), ảnh UI, dirty-state/provenance trước/sau, snapshot uncommitted SHA-256/bytes và baseline, hạn chế còn lại, GO/NO-GO tích hợp Soc_brain. Không tự công bố COMPLETED hoặc independent PASS; dừng tại đúng authority boundary. Nếu allocation/authority chặn, báo blocker thật, không tạo workspace/task thay thế.
