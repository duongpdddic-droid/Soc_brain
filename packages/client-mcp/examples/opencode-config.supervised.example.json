{
  "$schema": "https://opencode.ai/config.json",
  "//": "Task #183 — SUPERVISED OpenCode CLIENT config. Identical to opencode-config.example.json EXCEPT SOC_MCP_AUTO_RECOVER=1 (STRICT): a (re)spawned adapter reattaches ONLY to the previously pinned exact identity at boot and never attaches a task by discovery (missing pin => AUTO_RECOVERY_PIN_MISSING fail-closed), so the AUTO recovery supervisor can verify the SAME canonical task with zero operator tool calls. On a completely fresh plane, set 'bootstrap' once for the first attach, then return to '1'. Run the client as `opencode serve --port <pinned>` and launch the supervisor: SOC_OPENCODE_CONTROL_URL=http://127.0.0.1:<pinned> node packages/client-mcp/mcp-supervisor.mjs. Supervisor authority is transport-only (see packages/client-mcp/supervisor.mjs).",
  "mcp": {
    "soc-brain-client": {
      "type": "local",
      "command": [
        "node",
        "C:\\Users\\Admin\\Soc_brain\\packages\\client-mcp\\client-mcp.mjs"
      ],
      "environment": {
        "SOC_CONTROL_STATE_DIR": "C:\\Users\\Admin\\.soc-brain\\state",
        "SOC_CONTROL_WORKTREES_ROOT": "C:\\Users\\Admin\\.soc-brain\\worktrees",
        "SOC_CONTROL_LANE": "client-plane",
        "SOC_MCP_AUTO_RECOVER": "1"
      },
      "enabled": true
    }
  }
}
